<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.resturantfinder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminLoginPage" />
        <activity android:name=".FilterRestaurantList" />
        <activity android:name=".user_after_login" />
        <activity android:name=".FavoriteRestaurantList" />
        <activity android:name=".user_home" />
        <activity android:name=".add_rest" />
        <activity android:name=".admin_rest_list" />
        <activity android:name=".feedback" />
        <activity android:name=".RestaurantDetails" />
        <activity android:name=".Filter" />
        <activity android:name=".MainActivity_" />
        <activity android:name=".password_reset" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".LoginPage" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>